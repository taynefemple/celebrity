{
  "name": "celebrity-app",
  "version": "1.0.0",
  "description": "online celebrity front end",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server .",
    "format": "npx prettier --write ."
  },
  "author": "Wayne Temple",
  "license": "ISC",
  "dependencies": {
    "socket.io-client": "^3.1.0"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
